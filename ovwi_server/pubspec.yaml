name: ovwi_server
description: Open Verifiable Workflow Infrastructure server
version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  crypto: ^3.0.3
  cryptography: ^2.7.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6